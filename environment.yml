name: memoirs
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.12
  - nodejs>=22
  - pip
  - pip:
      - Django>=5.0,<6.0
      - Pillow>=10.0
      - qrcode[pil]>=7.4
      - dj-database-url>=2.2
      - django-simpleui>=2026.1
      - gunicorn>=22.0
      - psycopg[binary]>=3.2
      - whitenoise>=6.7
